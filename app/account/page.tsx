import {requireUser} from '@/app/auth';
import {adminEmails} from '@/lib/data-server';
export const dynamic='force-dynamic';
export default async function AccountPage({searchParams}: {searchParams: Promise<{denied?:string}>}){const user=await requireUser();const isAdmin=adminEmails().includes(user.email.toLowerCase());const {denied}=await searchParams;return <div className="mx-auto max-w-2xl space-y-5"><h1 className="text-3xl font-semibold">Your account</h1>{denied&&<p role="alert" className="rounded-md border border-amber-300 p-3 text-sm">That page is not enabled for your account. Ask an administrator to update your page access.</p>}<div className="rounded-lg border p-5"><p className="font-medium">{user.email}</p><p className="mt-1 text-sm text-muted-foreground">{isAdmin?'Administrator':'Manager'} · Signed in with Cloudflare Access</p></div>{isAdmin&&<a href="/account/users" className="inline-block rounded-md border px-4 py-2">Manage dashboard users</a>}<p><a href="/cdn-cgi/access/logout" className="text-sm underline">Sign out</a></p></div>;}
